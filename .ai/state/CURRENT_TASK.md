# Current task

The interface has been rebuilt against the operator's stated workflow and the Vault Prospector
shell. Stagecoach now opens directly onto a filterable machine list, pins a local account per
machine so connecting never prompts, uses one account for both Arc hops, keeps running in the
notification area while sessions are live, and updates itself from Settings.

Local verification is complete: Release build with zero warnings, 39 tests passing, formatting
clean, no vulnerable packages, docs site builds with no dead links, the application launches with a
responsive window, and the ZIP and MSI are produced.

Outstanding: publish a GitHub release so the documentation download page resolves, and validate the
live Azure paths against representative authorized estates. No work item should be closed on the
strength of the local build alone.
