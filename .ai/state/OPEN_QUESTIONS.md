# Open questions

1. v1 workstation target: Windows-only (recommended) or macOS tunnel fallback from day one?
2. Key Vault convention: kv-hcs-vault-01, per-tenant vaults, or per-VM `stagecoach-secret` tags only?
3. Surface "activate PIM role" links where scanning needs elevation?
4. ADO Epic/Feature for AB# references — not yet created.
